@page
@model AdminMembers.Pages.LoginModel
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<AdminMembers.Resources.SharedResources> Localizer
@{
    Layout = null;
    ViewData["Title"] = Localizer["SignIn"];
    var culture = System.Globalization.CultureInfo.CurrentUICulture.TwoLetterISOLanguageName;
}
<!DOCTYPE html>
<html lang="@culture">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@Localizer["SignIn"] - @Localizer["AppTitle"]</title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body class="login-wrap">
    <svg xmlns="http://www.w3.org/2000/svg" style="display:none"><symbol id="icon-shield" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></symbol><symbol id="icon-eye" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></symbol></svg>
    <div class="login-card">
        <div class="login-head">
            <svg class="icon icon-2xl" style="color:#6366f1;margin-bottom:12px;"><use href="#icon-shield" /></svg>
            <h1>@Localizer["AppTitle"]</h1>
            <p>@Localizer["MemberAdministration"]</p>
        </div>
        <div class="login-body">
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="login-error">@Model.ErrorMessage</div>
            }
            <form method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="RedirectUrl" />
                <div class="login-input-group">
                    <label for="Username">@Localizer["Username"]</label>
                    <input asp-for="Username" type="text" id="Username"
                           placeholder="@Localizer["EnterYourUsername"]"
                           autocomplete="username" autofocus required />
                </div>
                <div class="login-input-group">
                    <label for="Password">@Localizer["Password"]</label>
                    <div class="password-wrapper">
                        <input asp-for="Password" type="password" id="Password"
                               placeholder="@Localizer["EnterYourPassword"]"
                               autocomplete="current-password" required />
                        <button type="button" class="pwd-toggle" onclick="togglePwd()" aria-label="Toggle password visibility">
                            <svg class="icon icon-lg"><use href="#icon-eye" /></svg>
                        </button>
                    </div>
                </div>
                <button type="submit" class="btn-login" style="margin-top:12px;">@Localizer["SignIn"]</button>

                <div style="text-align:center;margin-top:12px;font-size:13px;">
                    <a href="/ForgotPassword" style="color:var(--accent);font-weight:600;">@Localizer["ForgotPassword"]</a>
                </div>

                <div style="text-align:center;margin-top:12px;font-size:13px;color:var(--text-muted);">
                    @Localizer["RequestAccount"]? <a href="/Register" style="color:var(--accent);font-weight:600;">@Localizer["Register"]</a>
                </div>
            </form>
        </div>
    </div>
    <script>
        function togglePwd() {
            const i = document.getElementById('Password');
            i.type = i.type === 'password' ? 'text' : 'password';
        }
    </script>
</body>
</html>
